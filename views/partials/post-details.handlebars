<section class="card">
    <header>{{title}}</header>

  <h3>Posted by: {{user}}</h3>  
  <h4>Posted on: {{format_time posts.posting_date}}</h4>
    <div class="card-body">
      <p>{{text}}</p>
    </div>
</section>